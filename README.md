# Room Fullness Device Program

## Prerequisites
* Python 3
* OpenCV (with Python 3 bindings)  
  *Note: On macOS with homebrew this can be installed via:*
  
  ```
  brew install opencv3 --without-python --with-python3
  brew ln opencv3 --force
  ```
